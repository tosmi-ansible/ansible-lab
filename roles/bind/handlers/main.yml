---
# handlers file for bind

- name: restart bind
  ansible.builtin.service:
    name: "{{ bind_service }}"
    state: restarted

- name: reload bind
  ansible.builtin.service:
    name: "{{ bind_service }}"
    state: reloaded

